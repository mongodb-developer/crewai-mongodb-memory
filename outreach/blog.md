# <Framework> + MongoDB Atlas: <Surface> in <N> minutes

> Drafted only after acceptance tests pass. Assets only — do not publish.

## The problem

<What breaks without durable/searchable state in <framework>.>

## What we built

<The adapter (class/package name), the extension point it implements, where it lives.>

## Architecture

<Collection schema, indexes (incl. Vector Search index if VS/RT), and the appName +
driver-info conventions — and why attribution matters.>

## Walkthrough

<Step-by-step from the demo: connect → seed `data/embeddings.json` → use the surface.
Include the real code snippets and the printed output.>

## Vector / hybrid search (if applicable)

<Embedding with voyage-3.5, the `$vectorSearch` (and `$rankFusion`) pipeline, sample results.>

## Testing

<What the acceptance suite checks and how to run it.>

## Try it

<Links: repo/PR, upstream docs, MongoDB Atlas signup, the demo directory.>
